name: deploy

on:
  workflow_dispatch:

  push:
    branches:
      - main

#  schedule:
#    - cron: '0 0 * * *'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare
        uses: djsora/rust_test_repo_dont_use_it_1337/.github/workflows/prepare@main

      - name: Build
        run: cargo run --release

#      - name: Minify
#      - name: Deploy