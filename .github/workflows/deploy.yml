name: deploy

on:
  workflow_dispatch:

  push:
    branches:
      - main

#  schedule:
#    - cron: '0 0 * * *'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Default Rust actions
        uses: ./.github/workflows/rust_reusable.yml

      - name: Build
        run: cargo run --release

#      - name: Minify
#      - name: Deploy